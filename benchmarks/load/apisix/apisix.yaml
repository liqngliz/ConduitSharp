# APISIX standalone route config — mirrors scenario-a.json: one catch-all,
# round-robin across the two nginx nodes. No plugins.
routes:
  - uri: /*
    upstream:
      type: roundrobin
      nodes:
        "upstream:8081": 1
        "upstream:8082": 1
#END
