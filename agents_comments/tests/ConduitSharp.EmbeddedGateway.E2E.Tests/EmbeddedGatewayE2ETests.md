# tests/ConduitSharp.EmbeddedGateway.E2E.Tests/EmbeddedGatewayE2ETests.cs

Inline commentary for the source file above, keyed by symbol. Read this before
changing that file; update it in the same change. Line numbers are where each note
sat when it was moved and are a hint only — the symbol is the anchor.

## EmbeddedGatewayE2ETests.PostUpload_WithStreamOnly_Succeeds_AndNoBodyCapture

- (was line 22) Uploads — streamOnly, no body capture
- (was line 37) Verify that BodyCapture did NOT log this route (it's not configured on the upload route)
